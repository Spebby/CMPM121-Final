using System;
using CMPM.Level;
using Newtonsoft.Json;

/*
namespace CMPM.Utils.LevelParsing {
    internal class SpawnDataParser : JsonConverter<Spawn> {
        public override Spawn ReadJson(JsonReader reader, Type objectType, Spawn existingValue, bool hasExistingValue, JsonSerializer serializer) {
            
        }

        public override void WriteJson(JsonWriter writer, Spawn value, JsonSerializer serializer) {
            writer.WriteValue(value.ToString().ToLowerInvariant());
        }
}
*/